# Culturaft Frontend

Install dependencies and run:

```powershell
cd frontend
npm install
npm run dev
```

By default the frontend expects the API at `http://localhost:5000/api`. You can change that by setting `VITE_API_URL` in `.env`.
