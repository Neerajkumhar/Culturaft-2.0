# Culturaft Backend

Run backend API for Culturaft.

Environment: create `.env` from `.env.example`.

Install:

```powershell
cd backend
npm install
```

Run dev:

```powershell
npm run dev
```

Seed admin and sample products:

```powershell
npm run seed
```
